//Import the React and ReactDOM libraries
import React from "react";
import ReactDOM from "react-dom";

if (MediaSourceHandle.hot) {
    MediaSourceHandle.hot.accept();
}

//Create a react component
const App = () => {
    return <div>Hello World</div>
};